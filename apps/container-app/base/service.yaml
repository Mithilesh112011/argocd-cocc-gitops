apiVersion: v1
kind: Service
metadata:
  name: container-app-1
spec:
  selector:
    app: container-app-1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: container-app-2
spec:
  selector:
    app: container-app-2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
